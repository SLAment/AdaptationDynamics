channels:
  - bioconda
  - defaults
  - conda-forge
  # - r # Some dependencies do not work together when I set this
dependencies:
  - r-vcfr=1.10.0
  - r-cowplot=1.0.0 # it comes with ggplot2 3.1.1
  - r-tidyr=0.8.3 # includs dplyr (the newer version creates conflicts)
  # - r-tidyr=1.1.0 # includs dplyr 1.0.0 
  - r-vegan=2.5_6